- DONE ----- > the exit builtin needs some work, check testcases in tester

- DONE ----- > the export builtin needs some work, check testcases in tester

- shoud be able to execute commands inside /bin even if the PATH is unset

- syntax error: `|` `<` `>` `<<<<` `>>>>>` `|echO|` `<>`

- DONE ---- > remove global variable

- DONE ----- > make ft_strtoll in the exit function norm conform

- start state before norm in bh_minus_1 :
418:STD_OUT: 39  STD_ERR: 12  EXIT_CODE: 31
# 	# 	# 	#
- Replace functions from external libraries used in minishell
# 	# 	# 	#

_time
# in		srand(time(NULL));  #in p_heredoc.c

_srand
# in		srand(time(NULL));  #in p_heredoc.c

_pclose
# in		pclose(fp); #in command_substitution.c


_popen
# in	FILE	*pipe_command(char *cmd) # in command_substitution.c

	fp = popen(cmd, "r");


_rand
# in		key = rand() % (int)(sizeof(charset) - 1); #ft_randstring.c


_realloc
# in				hn2 = realloc(*buf, newlen * sizeof(char **)); in utils.c

_DDONEEE   execv

# in	int	exec_cmd(int argc, char **argv)

		execv(argv[0], argv);

		execv(path, argv);

_fileno

# in	void	exit_shell(int exit_code)

_snprintf

_sprintf

	if (isatty(fileno(stdin)))

_fnmatch

# in	int	is_match_found(char *pattern, int longest, t_match *m)

		if (fnmatch(pattern, m->str, 0) == 0)

# in	void	check_pattern(char *pattern, int longest, t_match *m, char **s)

		if (fnmatch(pattern, m->str, 0) == 0)

_fread

# in	*read_from_pipe(FILE *fp, char *b, size_t *bufsz, char **p)

	i = fread(b, 1, 1024, fp);
		i = fread(b, 1, 1024, fp);

_getpwnam
# in	char	*find_home_from_pwnam(char *s)

	pass = getpwnam(s + 1);

_getpwuid
# in			pass = getpwuid(getuid()); # in tilde.c

_getuid
# in			pass = getpwuid(getuid()); # in tilde.c

_globfree
# in					globfree(&glob_info.glob); #in filename.c
# in			globfree(matches); # in pattern.c


_setenv
# in		setenv(name, new_value, 1); #in export.c

_DONE - strcpy

_DONE - strchr

_DONE - strcmp

_DONE - strlen


_DONE - strstr




Here are remaining minishell external functions names by the starting letter.


#### E
- DONE _execv

#### F
- DONE_fileno
- DONE_fnmatch
- DONE_fread

#### G
- _getpwnam
- _getpwuid
- _getuid
- _globfree

#### P
- _pclose
- _popen


#### R
- DONE _rand   418:STD_OUT: 40  STD_ERR: 12  EXIT_CODE: 31

#### S
- _snprintf
- _sprintf
- _srand

#### T
- _time


Here's "subject list" sorted by the starting letter of each name:

#### A
- _access
- _add_history

#### C
- _chdir
- _closedir
- _close

#### D
- _dup
- _dup2

#### E
- _execve
- _exit

#### F
- _fork
- _free
- _fstat

#### G
- _getcwd
- _getenv

#### I
- _ioctl
- _isatty

#### K
- _kill

#### L
- _lstat

#### M
- _malloc

#### O
- _open
- _opendir

#### P
- _perror
- _pipe
- _printf

#### R
- _read
- _readdir
- _readline
- _rl_clear_history
- _rl_on_new_line
- _rl_redisplay
- _rl_replace_line

#### S
- _sigaction
- _sigaddset
- _sigemptyset
- _signal
- _stat
- _strerror

#### T
- _tgetent
- _tgetflag
- _tgetnum
- _tgetstr
- _tgoto
- _tcgetattr
- _tcsetattr
- _ttyslot
- _tputs
- _ttyname

#### U
- _unlink

#### W
- _wait
- _wait3
- _wait4
- _waitpid
- _write

I hope this helps!
